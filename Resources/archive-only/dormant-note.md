# Dormant note (not consolidated)

This file exists so the folder isn't empty. Because the parent
`README.md` declares `include_in_consolidation: false`, the agent
will skip this entirely during `consolidate`. It's here for human
reference only.
