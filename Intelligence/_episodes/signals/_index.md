# Signal episodes

Derived from `Resources/context/` auto-capture drops — micro-episodes
of the user (a decision, a stated preference, a strong reaction).
Captured **inline during `consolidate`**, before the source is deleted
(`Resources/context/` is `delete_after_consolidation: true`, so a
post-run pass would find the source gone). Recall surfaces these as
personal context during generative/personal tasks.

## Episodes

- `signal-prefers-concise-prose` | tags: auto-capture, writing-style | captured a tone preference | success | distilled: false
