# Reflections

Distilled patterns across episodes. Generalized guidance, not
step-by-step. `reflect` **merges and rewrites** this list each run so
it stays small as the episode store grows — recall leans on these
compressed lines instead of re-reading many raw episodes.

<!-- format: `- **<category>:** <generalized heuristic>. (episodes: <id>, <id>)` -->

- **Routing:** a single-thesis source with no existing home routes
  best to a new dedicated topic, not a loosely-related one — routing
  stays legible. (episodes: operational-consolidate-2026-01-01T00-00-00Z)

_Placeholder reflection — replaced/merged on the first real `reflect` run._
